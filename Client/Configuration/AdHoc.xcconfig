BUNDLE_DISPLAY_NAME = $(TARGET_NAME) β

// Bundle Identifier
APP_BUNDLE_ID = $(PRODUCT_BUNDLE_IDENTIFIER).nightly

// URL Scheme
CLIQZ_URL_SCHEME = $(TARGET_NAME)Nightly

// User Activity Types
USER_ACTIVITY_TYPES= $(PRODUCT_BUNDLE_IDENTIFIER).activity.types.nightly
