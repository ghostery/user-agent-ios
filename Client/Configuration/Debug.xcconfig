BUNDLE_DISPLAY_NAME = $(TARGET_NAME) α

// Bundle Identifier
APP_BUNDLE_ID = $(PRODUCT_BUNDLE_IDENTIFIER).debug

// URL Scheme
CLIQZ_URL_SCHEME = $(TARGET_NAME)

// User Activity Types
USER_ACTIVITY_TYPES= $(PRODUCT_BUNDLE_IDENTIFIER).activity.types.debug
